確認点修正点など

【全ページ】
	・aタグの中にhタグ、1ページに同じidを複数使用等
		形式不備がいくつかあったので要確認
		（meetingList.htmlとminutes.htmlは直したので
			css崩れてないか等確認してください）
	・「日時」「開催日時」、「議題」「タスク名」など
		ラベル名の不一致
	・戻るボタン等のデザイン不一致
	・戻るボタンのコントローラ追加
		（entranceController.javaに共通で使えるものを記述）
	・その他ページ遷移コントローラ追加
		buttonのonclick、リンクにパスを書いてのページ移動は
		多分動かないので、aタグかinputに変更
		（ページ遷移のみはaタグでentranceController.javaに
		会議情報等を持って動くところはinputでindMeetingController.javaに）
	・inputタグのtype="button"をtype="submit"に変更
		（type="button"ではコントローラが使えない）
	・日時表記不一致
		（日付と時間で分けるのか日時セットで一つのデータか）
	・保存、追加、送信ボタンがあるものは
		formタグで囲ってinput type="submit"にしてほしい
		ついでにtype="text"のものに
		入力する値に対応したnameをつけてくれるととてもうれしい
		（変数名要確認）


【home.html】
	・一覧の会議から各会議メニューへの遷移（submit）追加
		（仮でボタン作っているので修正してください）
	・案内メール作成画面遷移時、先にMeetingsの
		コンストラクタ作成しないと多分エラーになるので注意
		（th:valueがnullで怒られるとおもう）


【meetingList.html】
	・タスク名、備考はどのデータを入れるのか要確認
	・DB取得ループの関係でdivタグclassの
		"meeting1""meeting2"…ができず
		ポップアップが崩れてしまったので修正（ごめん！）
	・画面長さ固定、スクロールバー追加してもいいかも？
	・ポップアップ表示の度に画面遷移
		していることになっているのは適切か確認
		（web上の戻るボタンで戻りにくい）
	・削除/確認はscriptで出来るのか検証


【meetingMenu.html】
	・各ページリンク方法変更
		（aタグ→formタグ input type="submit"）


【invite.html】
	・会社情報やユーザ情報のDB操作未実装のため
		会社名等空白にしてます
		（分かりにくかったらmodelいれてください…）
	・住所は手入力か会社情報としてDBに保存するのか
	・署名は手入力かこちらで文字列結合するのか
	・参加人数は手入力か参加者データからのカウント数を入れるか
		（後者の場合コントローラかモデルで処理）
	・件名がうまく入らない
		mtg_titleと"のご案内"を文字列結合しようとしたが
		mtg_titleの長さが100になっており会議名しか見えない
		（改行、空白の置換を試したが変わらないわからん）
	・資料追加、gmail遷移ボタンが動くかの検証
	・textareaタグで値取得可能か要検証


【minutes.html】
	・議事録のフォーマット、書き方要確認
		（今の形式だと追加で値の保存取得がいる？）
	・会議名表示箇所追加
		（<div class="title">議事録</div>の文字置き換え？）
	・textareaタグで値取得可能か要検証


上記html以外はまだspring要素入ってないです
途中でspring使うことにしたから使えないものが出てきてしまった
とてもごめんなさい


